version: '3.8'

services:
  discord-bot:
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - .env
    volumes:
      - ./Downloads:/app/Downloads
      - ./guild.configs:/app/guild.configs
    restart: always